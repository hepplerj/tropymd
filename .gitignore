node_modules/
*.log
.DS_Store

# Local install zips — built by the release workflow on tag push.
tropymd-v*.zip
tropy-plugin-markdown-v*.zip

# Local test harness, fixtures, and output. Runs are local-only — fixtures
# can contain real research material and are not committed.
test/
